import React from 'react';

const StubImage = () => <></>;
export default StubImage;

export const TerminalInfoProvider = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};
